window.SIDEBAR_ITEMS = {"fn":[["recover_pk","Recovers the PublicKey of the signer of the message given the message and the signature."],["sign","Creates a digital signature of a message given a SecretKey, like the node’s secret. A receiver knowing the PublicKey (e.g. the node’s id) and the message can be sure that the signature was generated by the caller. Signatures are EC recoverable, meaning that given the message and the signature the PublicKey of the signer can be extracted."],["verify","Verifies a message was signed by a PrivateKey that derives to a given PublicKey, given a message, a signature, and the PublicKey."]]};